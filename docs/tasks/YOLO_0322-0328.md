
# YOLO 钢材缺陷检测项目 — 总计划

> 创建日期：2026年3月22日 状态：Week 1 执行中 角色：简历核心项目（项目1/2）

---

## 一、项目定位

这是简历上的核心项目，目标岗位是外企 CV 方向（博世、西门子、Cognex 等）。

面试时这个项目承载两个任务：

1. 证明你有完整的「数据 → 训练 → 评估 → 部署 → 服务化」项目经验
2. 证明你能用业界主流工具（YOLO + ONNX + FastAPI + Docker）解决工业场景的实际问题

时间紧迫，追求快速出成果，不追求理论完美。先跑通 baseline，再逐步打磨。

---

## 二、项目目标

|项目|内容|
|---|---|
|数据集|NEU-DET 钢材表面缺陷数据集|
|缺陷类别|6 类：Crazing、Inclusion、Patches、Pitted Surface、Rolled-in Scale、Scratches|
|样本量|约 1800 张（每类约 300 张）|
|模型|YOLOv8n（nano 版本，轻量、边缘部署友好）|
|训练指标|mAP@0.5 > 0.70|
|部署验证|ONNX 导出 + Python ONNX Runtime 推理，精度对齐|
|服务化|FastAPI + Docker（简历里作为项目1的子项）|
|SAM 集成|YOLO 检测框 → SAM 精细分割（简历亮点）|
|最终交付|GitHub 精品项目（README + Demo GIF + 清晰结构）|

---

## 三、项目结构

```text
YOLO_Defect_Detection/
├── README.md                     # 项目说明（英文为主）
├── requirements.txt              # 依赖列表
├── LICENSE
├── data/
│   ├── data.yaml                 # YOLO 数据集配置
│   ├── images/
│   │   ├── train/
│   │   ├── val/
│   │   └── test/
│   └── labels/
│       ├── train/
│       ├── val/
│       └── test/
├── scripts/
│   ├── prepare_data.py           # 数据格式转换 + 划分
│   ├── data_analysis.py          # 数据分布分析 + 可视化
│   ├── train.py                  # 训练入口（封装 YOLO CLI）
│   ├── evaluate.py               # 评估 + 可视化
│   ├── export_onnx.py            # ONNX 导出
│   └── inference_onnx.py         # ONNX Runtime 推理
├── src/
│   ├── __init__.py
│   ├── detector.py               # YOLODetector 核心推理类（FastAPI 复用）
│   └── sam_integration.py        # YOLO + SAM 联动
├── api/
│   ├── app.py                    # FastAPI 服务入口
│   └── Dockerfile                # 容器化部署
├── configs/
│   └── train_config.yaml         # 超参数配置（从代码抽离）
├── runs/                         # 训练输出（.gitignore）
├── models/                       # 导出的 ONNX 模型
│   └── best.onnx
├── docs/
│   ├── experiment_log.md         # 实验记录（替代学习笔记）
│   └── assets/                   # README 用图（Demo GIF、PR 曲线等）
└── .gitignore
```


更加具体落地：

```
yolo_defect/
├── README.md
├── CLAUDE.md
├── LICENSE
├── requirements.txt
├── environment.yml
├── .gitignore
├── data/
│   ├── data.yaml
│   └── NEU-DET/                 # 原始数据（已含 train/validation）
├── scripts/
│   ├── prepare_data.py
│   ├── data_analysis.py
│   ├── train.py
│   ├── evaluate.py
│   ├── export_onnx.py
│   └── inference_onnx.py
├── src/
│   ├── __init__.py
│   └── detector.py
├── api/
│   └── .gitkeep                 # Week 2 再补 app.py / Dockerfile
├── configs/
│   └── train_config.yaml
├── models/
│   └── .gitkeep
├── docs/
│   ├── tasks/
│   ├── experiment_log.md
│   └── assets/
└── runs/
    └── .gitkeep
```

**设计说明：**

- `scripts/` 放一次性脚本（数据准备、训练、评估）
- `src/` 放可复用的推理逻辑（FastAPI 和 SAM 都会调用 `src/detector.py`）
- `api/` 放服务化相关代码（Week 2 再建）
- `configs/` 用 yaml 管理超参，调参时不用改代码，只改配置

---

## 四、技术栈

|工具|用途|
|---|---|
|ultralytics (YOLOv8)|训练 + 评估 + 导出|
|ONNX / onnxruntime|模型格式转换 + 推理部署|
|OpenCV|图像读取和可视化|
|matplotlib|PR 曲线、混淆矩阵绘制|
|segment-anything|SAM 精细分割（Week 1 Day 7）|
|FastAPI|API 服务化（Week 2）|
|Docker|容器化部署（Week 2）|

```bash
pip install ultralytics onnx onnxruntime opencv-python matplotlib
# SAM 和 FastAPI 在用到时再装
```

---

## 五、分步计划（项目蓝图）

### Step 1：数据准备

1. 下载 NEU-DET 数据集
2. 分析数据分布：每类样本数、图片尺寸、标注格式
3. 转换为 YOLO 格式（NEU-DET 原始是 VOC XML）
4. 划分 train/val/test（8:1.5:0.5 或 8:2）
5. 创建 `data.yaml`

**产出：** `data/` 结构完整，`data.yaml` 可用，数据分析截图保存到 `docs/assets/`

### Step 2：基线训练

```bash
yolo detect train model=yolov8n.pt data=data.yaml epochs=50 imgsz=640 batch=16
```

**产出：** baseline mAP@0.5 数值（预期 0.50-0.65），loss 曲线

### Step 3：调参优化

|实验|变量|说明|
|---|---|---|
|实验 A|imgsz|512 vs 640 vs 800|
|实验 B|学习率|lr0=0.01 vs 0.001|
|实验 C|增强策略|默认 vs 调整 mosaic/mixup|
|实验 D|模型大小|YOLOv8n vs YOLOv8s|

每组实验只改一个变量。用 `configs/train_config.yaml` 管理超参。

**产出：** mAP@0.5 > 0.70 的最优配置，完整对比表格

### Step 4：结果分析

1. PR 曲线（ultralytics 自动生成）
2. 混淆矩阵：哪些缺陷容易互相误判
3. 误检案例分析：3-5 个典型 case
4. 按类别 AP：哪类最难检测，为什么

**产出：** `docs/experiment_log.md` 完整记录

### Step 5：ONNX 导出 + 推理验证

```bash
yolo export model=runs/detect/train/weights/best.pt format=onnx simplify=True
```

1. 编写 `src/detector.py`（核心推理类）和 `scripts/inference_onnx.py`
2. 对比 PyTorch vs ONNX 精度（mAP 差异应 < 0.01）
3. 记录 FPS 对比（CPU + GPU）

**产出：** `best.onnx` + 精度对齐报告 + 性能对比表

### Step 6：SAM 集成

YOLO 检测 bbox → 取中心点作为 SAM prompt → 生成精细 mask

**产出：** 5 张 YOLO框 vs SAM精细mask 对比图

### Step 7：GitHub 美化

1. 英文 README（项目亮点 → Demo GIF → 快速开始 → 实验结果 → 技术栈）
2. 录制 60 秒 Demo GIF
3. 确保 3 行命令能跑通推理
4. requirements.txt + LICENSE

### Step 8：FastAPI + Docker（Week 2）

1. `POST /detect`：上传图片 → ONNX 推理 → 返回 JSON
2. `GET /health`：健康检查
3. Dockerfile 容器化
4. 简单压测记录 QPS

**产出：** Docker 镜像 + API 能跑通 + 性能数据

---

## 六、Week 1 执行日历（3.22 — 3.28）

### 本周交付物（周六晚自检）

- [x] NEU-DET 数据集已转为 YOLO 格式，train/val 划分完成
- [x] 数据分析报告：类别分布图 + 图片尺寸统计 + 1 段文字分析
- [x] YOLOv8n baseline 训练完成，mAP@0.5 记录
- [x] 至少 3 组调参对比实验（imgsz / lr / augment）
- [x] PR 曲线 + 混淆矩阵截图保存
- [ ] ONNX 导出 + Python ONNX Runtime 推理跑通，精度对齐
- [ ] SAM + YOLO 联动 Demo 跑通
- [ ] ViT 项目：model.py ~ inference.py 已读，能口头解释 TransUNet 架构
- [ ] LeetCode 本周完成 6 题（Easy 为主，C++ 提交）
- [ ] 浏览了 10+ 个目标公司的实习 JD

### 每日时间块模板（10h）

```
09:00 - 09:30  ☕ 晨间例行（30min）
               LeetCode 1题(C++) 或 C++语法练习

09:30 - 12:00  🔥 YOLO 核心（2.5h）

12:00 - 13:30  午饭 + 休息

13:30 - 18:00  🔥 YOLO 核心（4.5h）

18:00 - 19:00  晚饭 + 休息

19:00 - 19:30  📖 ViT 代码阅读（30min）

19:30 - 20:00  🌐 实习信息 / MWM 维护（30min，交替进行）

20:00 - 21:00  🗣️ 英语口语（30min）+ 复盘笔记（30min）

21:00          收工
```

---

### Day 1 — 3月22日（周日）：环境搭建 + 数据下载

**YOLO 核心（7h）：**

|时间|任务|操作|完成标志|
|---|---|---|---|
|09:30-10:30|创建项目仓库|按第三节的结构建目录，git init，写 .gitignore + requirements.txt|仓库就绪|
|10:30-12:00|下载 NEU-DET|Kaggle 或东北大学官网，解压到 `data/raw/`，浏览图片了解 6 类缺陷|数据在本地|
|13:30-15:30|VOC→YOLO 格式转换|Claude Code 写 `scripts/prepare_data.py`，你跑通并抽查输出|YOLO txt 标签生成|
|15:30-17:00|Train/Val 划分|在 prepare_data.py 里加划分逻辑，生成 `data.yaml`|data.yaml 可读取|
|17:00-18:00|安装 ultralytics|`pip install ultralytics`，跑 `yolo predict` 验证环境|预测出框|

**穿插任务：**

|时间|任务|
|---|---|
|09:00-09:30|LeetCode：1. Two Sum（C++，练 unordered_map）|
|19:00-19:30|ViT：打开 `model.py`，只看 class 定义和 `__init__`，画模块关系图|
|19:30-20:00|实习：注册 BOSS直聘 + 牛客，搜 "CV 算法 实习"，收藏 5 个 JD|
|20:00-20:30|英语：英文自我介绍 × 3 遍（计时 1 分钟）|
|20:30-21:00|复盘：写今天 3 个关键收获 + 明天计划|

---

### Day 2 — 3月23日（周一）：数据分析 + Baseline 训练

**YOLO 核心（7h）：**

| 时间          | 任务          | 操作                                                                                        | 完成标志       |
| ----------- | ----------- | ----------------------------------------------------------------------------------------- | ---------- |
| 09:30-11:30 | 数据分析        | Claude Code 写 `scripts/data_analysis.py`：每类样本数柱状图 + bbox 尺寸分布 + 图片尺寸。截图保存到 `docs/assets/` | 分析图生成      |
| 11:30-12:00 | 写分析摘要       | README 加 Data Analysis 章节（1 段文字）                                                          | README 更新  |
| 13:30-15:00 | Baseline 训练 | `yolo train model=yolov8n.pt data=data.yaml epochs=50 imgsz=640 batch=16`                 | 训练跑起来      |
| 15:00-16:00 | 等训练 + 学     | 看 ultralytics 源码了解训练流程，或看 YOLO 论文                                                         | 知道 5 个关键超参 |
| 16:00-18:00 | 分析 Baseline | 记录 mAP，截图 PR 曲线 + 混淆矩阵，保存推理结果图                                                            | 数据记录完毕     |

**穿插任务：**

|时间|任务|
|---|---|
|09:00-09:30|LeetCode：20. Valid Parentheses（C++，练 stack）|
|19:00-19:30|ViT：继续 `model.py` 的 `forward()`，理解数据流|
|19:30-20:00|MWM：检查群消息，有任务记下，没有就浏览代码 10min|
|20:00-20:30|英语：用英文描述 YOLO 项目（30 秒版）|
|20:30-21:00|复盘：baseline mAP 记录，思考调参方向|

---

### Day 3 — 3月24日（周二）：调参 Round 1（imgsz + lr）

**YOLO 核心（7h）：**

| 时间          | 任务             | 操作                                              | 完成标志 |
| ----------- | -------------- | ----------------------------------------------- | ---- |
| 09:30-10:30 | 实验 1：imgsz=512 | 其他参数不变                                          | 训练开始 |
| 10:30-12:00 | 实验 2：imgsz=800 | 其他参数不变（显存不够就跳过）                                 | 训练开始 |
| 13:30-15:00 | 实验 3：lr 对比     | lr0=0.001 vs baseline 的 0.01                    | 训练开始 |
| 15:00-16:30 | 整理对比表格         | 等训练完成，用 Markdown 表格记录到 `docs/experiment_log.md` | 表格完成 |
| 16:30-18:00 | 误检案例分析         | 找 10 张误检/漏检图，分析原因，保存到 `docs/assets/`            | 分析完成 |

**实验记录模板（写进 `docs/experiment_log.md`）：**

```markdown
| 实验 | imgsz | lr0 | epochs | mAP@0.5 | mAP@50-95 | 训练时间 | 备注 |
|------|-------|-----|--------|---------|-----------|---------|------|
| baseline | 640 | 0.01 | 50 | ? | ? | ? | YOLOv8n |
| exp1 | 512 | 0.01 | 50 | ? | ? | ? | |
| exp2 | 800 | 0.01 | 50 | ? | ? | ? | |
| exp3 | 640 | 0.001 | 50 | ? | ? | ? | |
```

**穿插任务：**

|时间|任务|
|---|---|
|09:00-09:30|LeetCode：21. Merge Two Sorted Lists（C++，练链表）|
|19:00-19:30|ViT：读 `dataset.py`，理解 patient-level split|
|19:30-20:00|实习：BOSS 直聘浏览 5 个外企 JD，记录技术要求|
|20:00-20:30|英语：回答 "What is YOLO and how does it work?"|
|20:30-21:00|复盘：对比实验初步结论|

---

### Day 4 — 3月25日（周三）：调参 Round 2（增强 + 模型）+ 最终训练

**YOLO 核心（7h）：**

| 时间          | 任务             | 操作                                           | 完成标志 |
| ----------- | -------------- | -------------------------------------------- | ---- |
| 09:30-11:00 | 实验 4：增强策略      | 最优 imgsz+lr 基础上，mosaic=1.0, mixup=0.1        | 训练开始 |
| 11:00-12:00 | 实验 5：关闭增强      | mosaic=0, mixup=0，其他不变                       | 训练开始 |
| 13:30-15:00 | 最终训练           | 所有最优参数，epochs=100                            | 训练开始 |
| 15:00-16:30 | YOLOv8n vs v8s | 如果 n 达不到 0.70，换 v8s 跑一组                      | 对比数据 |
| 16:30-18:00 | 汇总所有实验         | 更新 `experiment_log.md`（至少 6 组），选出 best model | 表格完整 |

**穿插任务：**

|时间|任务|
|---|---|
|09:00-09:30|LeetCode：121. Best Time to Buy and Sell Stock（C++）|
|19:00-19:30|ViT：读 `losses.py`，理解 Dice + BCE 混合损失|
|19:30-20:00|MWM：检查群消息 + 处理任务（如果有）|
|20:00-20:30|英语：回答 "Why did you choose YOLOv8 over v5?"|
|20:30-21:00|复盘：最终实验结果|

---

# Day 5-Day7（重构版 — 2026-03-27 更新）

> **核心调整：砍掉 SAM 集成，提前做 FastAPI + Docker，确保项目端到端完整。**
>
> 理由：外企实习 JD 高频出现的是"部署、API、容器化"，不是 SAM。
> 完整工程链路（数据→训练→ONNX→API→Docker）比花哨的技术堆砌更有说服力。
> SAM 放到 README 的 Future Work 里，面试时说"计划集成"展示视野即可。

## 已完成的工作

|任务|状态|说明|
|---|---|---|
|`src/detector.py` 核心推理类|✅ 已完成|Claude Code 编写，包含手写 NMS|
|`scripts/inference_onnx.py` CLI 脚本|✅ 已完成|360 张验证集全部跑通|
|ONNX 导出|✅ 已完成|`models/best.onnx`，11.77 MB，imgsz=800|
|ONNX CPU 推理 FPS|✅ 已有数据|22.5 FPS（i7-12700KF，CPU）|

---

## Day 5 — 3月27日（周五）：精度对齐 + 理解推理链路

> 核心目标：PyTorch vs ONNX 精度对齐验证 + 你能脱稿讲清 detector.py

### YOLO 核心任务（7h）

#### 上午：精度对齐（Claude Code 执行）+ 你理解代码（3h）

|时间|任务|谁来做|操作细节|完成标志|
|---|---|---|---|---|
|09:30-10:30|PyTorch 推理 mAP + FPS|**Claude Code**|用 ultralytics `model.val()` 跑验证集 mAP@0.5/50-95，用 `model.predict()` 跑 100 张图测 FPS|PyTorch 数据齐全|
|10:30-11:00|ONNX mAP 近似验证|**Claude Code**|抽 50 张验证集图，对比 PyTorch 和 ONNX 的检测框数量、置信度分布，确认一致性|对齐报告|
|11:00-12:00|**你通读 detector.py**|**你自己**|逐行阅读 `preprocess()`、`predict()`、`_nms()`，遇到不懂的标行号问 Claude Code|能口头描述每个方法做了什么|

**你需要闭卷回答的 3 个验证问题**（写在笔记里）：

1. ONNX 输出形状 `[1, 10, 13125]` 每个维度什么意思？13125 怎么来的？
2. `preprocess()` 做了哪 5 步？顺序搞错了会怎样？
3. NMS 的 4 步流程？为什么 O(N²) 在实际中不是瓶颈？

#### 下午：性能对比表 + 代表性结果图 + 文档更新（4h）

|时间|任务|谁来做|操作细节|完成标志|
|---|---|---|---|---|
|13:30-14:00|**你填写**性能对比表格|**你自己**|根据上午数据填入 Markdown 表格，写 2-3 句分析|表格 + 分析完成|
|14:00-15:00|挑选 12 张代表性结果图|**你挑选，Claude Code 复制到指定目录**|从 `results/` 中每类挑 2 张（1 正确 + 1 误检/漏检），保存到 `docs/assets/showcase/`|12 张图就位|
|15:00-15:30|改 conf/iou 参数看效果|**你自己**|用 inference_onnx.py 分别设 `--conf 0.1/0.5` `--iou 0.3/0.7`，观察框数量变化，总结参数作用|理解阈值含义|
|15:30-17:00|更新 YOLO_Project.md Step 5|**你写分析文字，Claude Code 更新文件**|记录：做了什么、对比数据、面试知识点|文档更新|
|17:00-18:00|更新 README 性能数据|**Claude Code**|填充 README 中 ONNX 性能表格、推理示例图链接|README 部分更新|

**性能对比表格（你来填）：**

```markdown
| 格式 | mAP@0.5 | mAP@50-95 | FPS (CPU) | 模型大小 | 备注 |
|------|---------|-----------|-----------|----------|------|
| PyTorch (.pt) | ? | ? | ? | ? MB | ultralytics model.val() |
| ONNX (.onnx) | ? | ? | 22.5 | 11.77 MB | detector.py + onnxruntime |
| 差异 | ? | ? | ? | ? | 差异 < 0.01 为正常 |
```

### 穿插任务

|时间|任务|
|---|---|
|09:00-09:30|LeetCode：53. Maximum Subarray（Medium，DP 入门）|
|19:00-19:30|ViT：读 `train.py`，对比 YOLO 训练循环找异同|
|19:30-20:00|实习：去博世/西门子/Cognex 官网看实习岗位|
|20:00-20:30|英语：回答 "What is ONNX and why do we need it?"|
|20:30-21:00|复盘：闭卷口述 detector.py 推理链路|

---

## Day 6 — 3月28日（周六）：FastAPI 服务 + README 完善

> 核心目标：用 `src/detector.py` 搭建 REST API，README 用真实数据填完
>
> 为什么提前做 FastAPI？因为"训练→导出→API服务化"是外企面试最看重的完整链路。

### YOLO 核心任务（7h）

#### 上午：FastAPI 最小可用版（3h）

|时间|任务|谁来做|操作细节|完成标志|
|---|---|---|---|---|
|09:30-10:30|FastAPI 核心代码|**Claude Code**|写 `api/app.py`：`POST /detect` 上传图片→ONNX推理→返回JSON（bbox+class+conf），`GET /health` 健康检查。直接复用 `src/detector.py`|代码完成|
|10:30-11:00|本地测试 API|**Claude Code 启动，你用 curl/浏览器测**|启动 `uvicorn api.app:app`，用 curl 上传一张图片，确认返回 JSON 正确|API 能跑|
|11:00-11:30|添加可视化返回模式|**Claude Code**|`POST /detect?visualize=true` 返回画了框的图片（PNG），方便 Demo|可视化模式可用|
|11:30-12:00|**你理解 API 代码**|**你自己**|阅读 `api/app.py`，画出请求流程：HTTP请求 → FastAPI路由 → detector.predict() → JSON响应。能口头描述|理解API架构|

**面试关键点**（你要能说清楚）：
- 为什么用 FastAPI 而不是 Flask？（async 支持、自动生成 Swagger 文档、类型校验）
- API 的请求/响应格式是什么？
- `detector.py` 被 inference 脚本和 API 同时复用 — 这就是封装成类的价值

#### 下午：README 英文版 + Demo + Git 推送（4h）

|时间|任务|谁来做|操作细节|完成标志|
|---|---|---|---|---|
|13:30-14:00|录制 Demo GIF|**你自己**|用 ScreenToGif：终端跑 `inference_onnx.py` 单张推理 → 展示结果图。30-60 秒|demo.gif 生成|
|14:00-16:00|README 全面更新|**Claude Code 更新文件**|填充所有 TODO：mAP 数值、FPS、模型大小、PR 曲线图、混淆矩阵图、实验对比表、Demo GIF、Quick Start 命令、项目结构树。英文为主|README 完整|
|16:00-17:00|requirements.txt + LICENSE|**Claude Code**|生成准确的 requirements.txt（从实际 import 推导），添加 MIT LICENSE|文件就位|
|17:00-18:00|Git 整理 + Push|**Claude Code 协助，你做最终确认**|确认 `.gitignore` 正确、commit message 有意义、push 到 GitHub|仓库公开可访问|

**README 检查清单（你逐项确认）：**

- [ ] Demo GIF 或 Demo 截图显示正常
- [ ] Quick Start 的 3 行命令能跑通（clone → install → inference）
- [ ] 实验结果表格有真实数据
- [ ] PR 曲线和混淆矩阵图片链接正确
- [ ] ONNX 性能对比表有数据
- [ ] API 使用示例（curl 命令）
- [ ] 项目结构树与实际一致

### 穿插任务

|时间|任务|
|---|---|
|09:00-09:30|LeetCode：217. Contains Duplicate（C++，练 unordered_set）|
|19:00-19:30|ViT：读 `inference.py`，理解 unsqueeze + softmax + argmax|
|19:30-20:00|MWM：常规检查 / 周报（如果需要）|
|20:00-20:30|英语：用英文描述 "How does your detection API work?"（30 秒版）|
|20:30-21:00|复盘：FastAPI 架构的面试话术|

---

## Day 7 — 3月29日（周日）：Docker + 面试模拟 + Week 2 规划

> 核心目标：Docker 容器化让项目可一键部署，面试模拟验证掌握程度

### 上午：Docker 容器化 + 查漏补缺（3h）

|时间|任务|谁来做|操作细节|完成标志|
|---|---|---|---|---|
|09:30-10:30|Dockerfile + docker-compose|**Claude Code**|写 `api/Dockerfile`：基于 python:3.9-slim，安装依赖，COPY 模型+代码，暴露 8000 端口。写 `docker-compose.yml` 一键启动|文件完成|
|10:30-11:00|构建 + 测试容器|**Claude Code 构建，你测试**|`docker build` + `docker run`，curl 测试 API|容器能跑|
|11:00-11:30|**你理解 Docker 相关文件**|**你自己**|阅读 Dockerfile 每一行的作用，理解镜像分层。能口头描述：基础镜像→依赖安装→代码复制→启动命令|理解容器化流程|
|11:30-12:00|Week 1 交付物逐项自检|**你自己**|对照清单确认，缺的标红|清单基本全绿|

**Week 1 交付物清单（更新版）：**

|#|交付物|状态|
|---|---|---|
|1|NEU-DET 数据转 YOLO 格式，data.yaml 可用|✅|
|2|数据分析报告：类别分布图 + bbox 统计 + 文字分析|✅|
|3|YOLOv8n baseline 训练完成，mAP@0.5=0.734|✅|
|4|9 组调参实验，best mAP@0.5=0.743|✅|
|5|PR 曲线 + 混淆矩阵截图|✅|
|6|ONNX 导出 + ONNX Runtime 推理跑通，22.5 FPS|✅|
|7|PyTorch vs ONNX 精度对齐报告|Day 5 完成|
|8|FastAPI 检测 API（POST /detect + GET /health）|Day 6 完成|
|9|GitHub README 完整（英文、真实数据、Demo）|Day 6 完成|
|10|Docker 容器化一键部署|Day 7 完成|
|11|experiment_log.md 实验记录完整|✅|

### 下午：面试模拟 + 知识巩固（3h）

|时间|任务|谁来做|操作细节|完成标志|
|---|---|---|---|---|
|13:30-14:30|**YOLO 项目面试模拟（10 题）**|**你回答，Claude 当面试官**|闭卷口答，不看任何文档。答完后 Claude 逐题打分并纠正|至少 7/10 通过|
|14:30-15:00|补强弱项|**你自己**|答不上来的题，回去翻文档复习，用自己的话重新写答案|弱项补完|
|15:00-15:30|ViT/TransUNet 面试模拟（5 题）|**你回答，Claude 当面试官**|闭卷口答|至少 3/5 通过|
|15:30-16:00|C++ 语法复盘|**你自己**|回顾本周 LeetCode 用到的 STL|笔记更新|

**YOLO 面试模拟题库（更新版，含部署题）：**

```
基础概念（必须全对）：
1. YOLO 是一阶段还是两阶段？和 Faster RCNN 区别？
2. mAP@0.5 = 0.743 是什么意思？用你的项目数据举例
3. NMS 是什么？4 步流程说一遍
4. 你的 ONNX 输出 [1, 10, 13125] 每个维度什么意思？

项目深入（答出来加分）：
5. 你做了哪些调参实验？哪个参数影响最大？
6. 混淆矩阵里哪两个类最容易混淆？
7. ONNX 导出后精度有变化吗？可能原因？
8. detector.py 的 preprocess() 做了哪 5 步？

部署 + 工程（展示完整能力）：
9. 为什么封装 YOLODetector 类？它被哪些地方复用？
10. 你的 FastAPI 接口怎么设计的？请求/响应格式是什么？
11. Docker 镜像怎么构建的？为什么用 python:3.9-slim 而不是 full？
12. 如果要提升推理速度，你会怎么做？（TensorRT、量化、batching）
```

### 晚上：Week 2 规划（2h）

|时间|任务|谁来做|
|---|---|---|
|16:30-17:00|整理实习 JD 表格|你自己|
|17:00-17:30|评估 Week 1 完成质量，决定 Week 2 重点|你自己|
|17:30-18:00|写 Week 2 计划初稿|Claude 协助|
|19:00-19:30|英语：1 分钟自我介绍 + YOLO 项目描述（综合版）||
|19:30-20:00|MWM：常规检查||
|20:00-21:00|最终复盘：写下本周 3 个最大收获 + 3 个待改进||

**Week 2 方向（FastAPI/Docker 已在 Week 1 完成的情况下）：**

|优先级|Week 2 重点|
|---|---|
|P0|简历制作 + 开始海投（边投边改）|
|P1|项目打磨：压测 QPS 数据、错误处理、日志|
|P2|ViT/TransUNet 项目快速推进（第二个简历项目）|
|P3|SAM 集成（如果有余力，作为 bonus 亮点）|

---

# Claude Code 协作指令模板

以下是你在推进 Day 5-7 时，可以直接复制粘贴给 Claude Code 的指令：

### Day 5 — 精度对齐

```
我们在做 ONNX 精度对齐。请完成以下任务：

1. 用 ultralytics 的 model.val() 跑 best model（final_train_2）在验证集上的 mAP，
   记录 mAP@0.5 和 mAP@50-95

2. 用同样的验证集，跑 PyTorch 推理的 FPS（至少 100 张图取平均）

3. 把结果和已有的 ONNX 数据（mAP 待测，FPS=22.5，size=11.77MB）
   一起整理成 Markdown 对比表格

4. 完成后更新 docs/YOLO_Project.md 的 Step 5 部分
```

### Day 6 — SAM 集成

```
我们要做 YOLO + SAM 联动。请完成以下任务：

1. 安装 segment-anything，下载 vit_b 权重
2. 写 src/sam_integration.py：
   - 输入：一张图片路径
   - 流程：YOLO 检测 → 取每个 bbox 中心点 → SAM point prompt → 生成 mask
   - 输出：原图 + YOLO 框 + SAM mask 的叠加可视化
3. 跑 6 张图（每类 1 张），生成对比可视化保存到 docs/assets/
4. 完成后给我讲解代码，然后更新 docs/YOLO_Project.md
```

### Day 6 — README 更新

```
README 需要用真实数据更新。请完成：

1. 填充所有 TODO 和占位符（mAP、FPS、模型大小等）
2. 添加 PR 曲线和混淆矩阵的图片链接
3. 添加 Demo GIF（我会提供 demo.gif 路径）
4. 添加 SAM 对比图
5. 确认项目结构树与实际目录一致
6. 确认 Quick Start 的命令能跑通
```

---

# 关键提醒

1. **Day 5 上午是你自己动手时间**——通读 detector.py、打印中间值、改参数看效果。这不是 Claude Code 的任务，是你面试前的必修课
2. **精度对齐如果差异 > 0.01**——先检查 detector.py 的预处理是否和 ultralytics 一致（特别是 letterbox padding 和直接 resize 的区别）
3. **SAM 集成是展示级**——面试时能讲清楚"YOLO 定位 + SAM 分割"的联动思路就够了，不需要深入 SAM 的架构细节
4. **Day 7 面试模拟不能跳过**——这是验证你一周学习成果的唯一方式。答不上来的题说明有盲区，还来得及补
5. **优先级永远是：项目能跑 > README 好看 > 面试细节**——如果时间不够，SAM 可以简化为 3 张对比图，Demo GIF 可以用静态截图替代

---

# 七、穿插任务周汇总

### LeetCode（6 题，C++）

|Day|题目|考点|难度|
|---|---|---|---|
|1|1. Two Sum|HashMap|Easy|
|2|20. Valid Parentheses|Stack|Easy|
|3|21. Merge Two Sorted Lists|链表|Easy|
|4|121. Best Time to Buy/Sell Stock|数组|Easy|
|5|53. Maximum Subarray|DP 入门|Medium|
|6|217. Contains Duplicate|HashSet|Easy|

### ViT 代码阅读（每天 30min）

|Day|文件|重点|
|---|---|---|
|1|model.py（上半）|class 定义 + `__init__`，画模块图|
|2|model.py（下半）|`forward()` 数据流|
|3|dataset.py|patient-level split 逻辑|
|4|losses.py|Dice + BCE 混合损失|
|5|train.py|训练循环 vs YOLO 训练循环异同|
|6|inference.py|推理链路|
|7|面试模拟|Claude 问 5 个问题|

### 英语口语（每天 30min）

|Day|内容|
|---|---|
|1|英文自我介绍 × 3 遍|
|2|描述 YOLO 项目（30 秒）|
|3|回答 "What is YOLO?"|
|4|回答 "Why YOLOv8 over v5?"|
|5|描述 TransUNet 项目（30 秒）|
|6|回答 "What is ONNX?"|
|7|综合：1 分钟自我介绍 + 项目|

### 实习信息搜集（单日）+ MWM 维护（双日）

|Day|任务|
|---|---|
|1|实习：注册平台 + 搜 5 个 JD|
|2|MWM：检查群消息|
|3|实习：再看 5 个 JD|
|4|MWM：处理任务（如有）|
|5|实习：博世/西门子/Cognex 官网|
|6|MWM：常规维护|
|7|实习：汇总 JD 表格|

---

# 八、应急预案

**训练时间超预期（GPU 慢）：**

- 减少 epochs（50→30），先拿 baseline
- 晚上睡前开跑长训练，第二天早上看结果
- 等训练时做穿插任务，不要干等

**NEU-DET 下载不了：**

- 备选：GC10-DET（钢材缺陷，类似场景）
- 或 Roboflow 上的 NEU-DET 预处理版本

**MWM 小导师突然布置紧急任务：**

- 当天 19:00-21:00 全给 MWM
- YOLO 延后一天，不打乱整体节奏

**某天效率很低 / 状态不好：**

- 砍掉所有穿插任务，只保留 YOLO 核心
- 最低交付：每天至少推进 1 个 YOLO 任务项

**进度超前（AI 效率高）：**

- 不要停，直接开始 Week 2 的 FastAPI + Docker
- 提前投递简历永远比晚投好

---

# 九、Week 2 预告（3.29 — 4.04）

- FastAPI 服务化（`POST /detect` 接口）
- Docker 容器化
- 坏样本分析 + 主动学习模拟脚本
- README 最终美化 + GitHub 上线
- 开始简历制作

---

# 十、简历话术模板

### 中文版

**工业缺陷检测系统**（2026.03）

- 基于 YOLOv8 构建钢材表面缺陷检测系统，覆盖 6 类常见缺陷，mAP@0.5 = 0.XX
- 完成数据分析、模型调优、PR 曲线分析、混淆矩阵误检分析等全流程
- 集成 SAM 辅助精细分割，标注效率提升 XX%
- 导出 ONNX 格式，ONNX Runtime 推理验证精度对齐，CPU 推理速度提升 XX%
- 基于 FastAPI 开发检测 API 服务，Docker 容器化部署，QPS 达 XX
- 技术栈：Python / PyTorch / YOLOv8 / SAM / ONNX / FastAPI / Docker
- GitHub: [链接]

### English Version

**Industrial Defect Detection System** (2026.03)

- Built a YOLOv8-based defect detection system for 6 steel surface defect types, achieving mAP@0.5 = 0.XX on NEU-DET
- End-to-end pipeline: data analysis, hyperparameter tuning, PR curve analysis, confusion matrix-based error analysis
- Integrated SAM for fine-grained segmentation, improving annotation efficiency by XX%
- Exported to ONNX with ONNX Runtime inference validation; CPU inference speed improved by XX%
- Developed RESTful detection API with FastAPI, containerized with Docker, achieving XX QPS
- Tech: Python / PyTorch / YOLOv8 / SAM / ONNX / FastAPI / Docker
- GitHub: [link]

**注意：** XX 处等项目完成后用真实数据替换。




---
