# NEU-DET Steel Surface Defect Dataset - YOLO Format
# Auto-generated by scripts/prepare_data.py
# Run `python scripts/prepare_data.py` to populate images/ and labels/

path: ../data               # Dataset root (relative to project root)
train: images/train          # Train images (relative to path)
val: images/val              # Val images (relative to path)

# Class names
names:
  0: crazing
  1: inclusion
  2: patches
  3: pitted_surface
  4: rolled-in_scale
  5: scratches

nc: 6                        # Number of classes
