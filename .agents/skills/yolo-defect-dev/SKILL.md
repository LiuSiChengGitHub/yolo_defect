---
name: yolo-defect-dev
description: Locate recorded yolo_defect toolchain paths, dependency versions or locations, environment entry commands, and known environment pitfalls when the current task needs that information. Do not use for project work that does not require environment lookup.
---

# YOLO Defect Environment Index

- When the current task needs a known toolchain path, dependency version or location, environment entry command, or recorded environment pitfall, read only the relevant section of [the canonical environment reference](../../../docs/paths_commands.md) before searching the machine.
- Prefer the recorded information when it answers the question. Use discovery commands only when the document is missing the needed fact, appears stale or inconsistent, or conflicts with an actual command result.
- After obtaining the needed environment information, continue the user's task normally. This skill does not prescribe how the rest of the task is performed.
